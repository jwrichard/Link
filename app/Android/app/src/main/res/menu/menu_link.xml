<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="ca.justinrichard.link.MainActivity">
    <item
        android:id="@+id/action_notify"
        android:orderInCategory="100"
        android:title="Notify Link members"
        android:icon="@drawable/ic_action_notify"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/action_pause"
        android:orderInCategory="200"
        android:title="Pause updates"
        android:icon="@drawable/ic_action_pause"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/action_resume"
        android:orderInCategory="300"
        android:title="Resume updates"
        android:visible="false"
        android:icon="@drawable/ic_action_resume"
        app:showAsAction="ifRoom"/>
</menu>
